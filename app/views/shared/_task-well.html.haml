.well{class: "list-#{list.id}-well"}
  = div_for list do
    %h5= list.name
    //========== Task form ============
    = render partial: "tasks/form", locals: { list: list, date: date }
    //========== Tasks ============
    - classes = "list-#{list.id}-tasks task-sort"
    = content_tag :div, class: classes, data: { update_url: sort_tasks_url } do
      = render list.tasks_by_date(date)

= render partial: 'lists/edit-modal'
