= content_tag(:li, class: "category-#{category.id}") do
  = link_to username_category_path(@user.username, category.name, date: params[:date]), data: { category_id: category.id } do
    = category.name
